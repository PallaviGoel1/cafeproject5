//   Add to wishlist product function

var addToWishList = document.querySelector('#add-to-wishlist');

addToWishList.addEventListener('submit', function(event){
    if(addToWishList.value.length < 1) return;
        

});